@using MudBlazor
@using WSService.Services
@using Microsoft.AspNetCore.Components.Routing


@inherits LayoutComponentBase
@inject NavigationManager NavManager
@inject WSService.Services.ThemeService ThemeService
@inject Blazored.SessionStorage.ISessionStorageService SessionStorage


<MudThemeProvider />
    <MudLayout> 
        @* <MudAppBar Color="Color.Primary">
            <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="ToggleSidebar" />
            <MudText Typo="Typo.h6" Class="ml-2">WEATHERAPP</MudText>
        </MudAppBar >  *@

        @* <MudDrawer @bind-Open="@isSidebarOpen" Variant="DrawerVariant.Temporary">
            <MudNavMenu>
                <MudNavLink Href="/" Icon="@Icons.Material.Filled.Home">Home</MudNavLink>
                <MudNavLink Href="/profile" Icon="@Icons.Material.Filled.Person">Profile</MudNavLink>
            </MudNavMenu>
        </MudDrawer> *@

        <MudMainContent>
            @Body
        </MudMainContent>
        
        <MudPopoverProvider />
        <MudDialogProvider />
        <MudSnackbarProvider />
    </MudLayout>


@code {
    private bool isSidebarOpen = false;

    private void CloseSidebar() => isSidebarOpen = false;

    
    

    private void ToggleSidebar()
    {
        isSidebarOpen = !isSidebarOpen;
    }
} 